<%= form_for(@focus) do |f| %>
  <% if @focus.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@focus.errors.count, "error") %> prohibited this focus from being saved:</h2>

      <ul>
      <% @focus.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br />
    <%= f.text_field :content %>
  </div>

  <div class="field">
    <%= f.label :focus_at %><br />
    <%= f.datetime_select :focus_at %>
  </div>

  <div class="field">
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
